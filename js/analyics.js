// WHY: Pure functions only. No document.*, no mutation of inputs.
// Not part of FR-1 — stub for FR-3 (grade categorization) later.

function categorizeStudents(students) {
  // TODO (FR-3, not now): map/filter/reduce over `students`, return a
  // NEW object grouping names into A/B/C. Do not mutate `students`.
}

function averageGrade(students) {
  // TODO (FR-2, not now): reduce() to a single average value.
}

export { categorizeStudents, averageGrade };
